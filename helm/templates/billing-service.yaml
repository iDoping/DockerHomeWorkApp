{{- if .Values.billingService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: billing-service
  labels:
    app: billing-service
    app.kubernetes.io/part-of: docker-homework-app
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app: billing-service
{{- end }}
