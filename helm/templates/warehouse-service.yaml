{{- if .Values.warehouseService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: warehouse-service
  labels:
    {{- include "docker-homework-app.labels" . | nindent 4 }}
    app: warehouse-service
    app.kubernetes.io/part-of: docker-homework-app
    app.kubernetes.io/component: warehouse
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app: warehouse-service
{{- end }}